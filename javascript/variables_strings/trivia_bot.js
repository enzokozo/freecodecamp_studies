// Initial greeting
console.log("Hello! I'm your coding fun fact guide!");

// Configuring variables
const botName = "EnzoBot";
const botLocation = "Itajuba, MG";
const favoriteLanguage = "JavaScript";

// Introducing the bot
console.log(`My name is ${botName} and I live on ${botLocation}.`);
console.log(`My favorite programming language is ${favoriteLanguage}.`);

// Sharing first fact
let codingFact = `The first version of ${favoriteLanguage} was created in just 10 days by Brendan Eich in 1995.`;
console.log(codingFact);

// Sharing second fact
codingFact = `Many people confuse ${favoriteLanguage} with Java, but they are actually quite different languages.`;
console.log(codingFact);

// Sharing third fact
codingFact = `At the moment, ${favoriteLanguage} is the most popular programming language for web development in the world.`;
console.log(codingFact);

// Farewell message
console.log(`It was fun sharing these facts with you. Goodbye! - ${botName} from ${botLocation}.`);